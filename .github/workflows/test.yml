name: Test

on:
  pull_request:
    branches:
      - '*'
    paths:
      - '**/test.py'

jobs:
  test:
    runs-on: ubuntu-latest
    needs: Train
    
    steps:
      - name: Pull Docker image
        run: docker pull yourdockerhubusername/yourimagename:latest
      
      - name: Run Docker container
        run: docker run yourdockerhubusername/yourimagename:latest
      
      # You'll need to replace this with your actual test script that checks the score
      - name: Run Test Script
        run: python test.py
